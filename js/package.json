{
  "name": "coordo",
  "version": "1.0.0",
  "description": "A set of modules to help with using spatio-temporal data and creating maps",
  "license": "ISC",
  "author": "",
  "type": "module",
  "browser": "dist/map-plugin.browser.js",
  "main": "map-plugin.js",
  "scripts": {
    "build": "browserify map-plugin.js -o dist/map-plugin.browser.js -t browserify-css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "maplibre-gl": "^5.16.0"
  },
  "devDependencies": {
    "browserify": "^17.0.1",
    "browserify-css": "^0.15.0"
  }
}
