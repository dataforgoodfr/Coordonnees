# Python

## Schemas

The DataPackage schema have been generated with the following command :

```
uv run --with jsonschema-gentypes jsonschema-gentypes --json-schema ../schemas/datapackage.json --python coordo/types/datapackage.py
```
